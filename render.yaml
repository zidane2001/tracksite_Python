services:
  # Full-stack application (Backend + Frontend)
  - type: web
    name: tracksite-python5
    runtime: docker
    dockerfilePath: ./Dockerfile
    plan: free
    # Removed disk configuration - using alternative persistence method